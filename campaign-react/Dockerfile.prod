# Production Dockerfile for Next.js (dynamic/SSR)
FROM node:22-alpine as build
WORKDIR /app
COPY package*.json ./
RUN npm install --legacy-peer-deps
COPY . .
RUN npm run build

FROM node:22-alpine as runner
WORKDIR /app
COPY --from=build /app ./
EXPOSE 3000
CMD ["npm", "start"]